//Remove String Spaces
//remove the spaces from the string, then return the resultant string.

function noSpace(x){
  let newString = x.replace(' ', '')
  
  for(let i=0; i <= x.length; i++){

   newString = newString.replace(' ', '')
    console.log(newString)
 }
  return newString
   console.log(newString)
} 
 